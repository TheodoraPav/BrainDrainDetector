# ============================================================
# Experiment 1 — Baseline (no augmentation, classification)
# ============================================================
defaults:
  - base

augmentation:
  enabled: false
