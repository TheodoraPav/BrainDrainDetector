# ============================================================
# Experiment 1 — Baseline (no augmentation, binary Safe/Alarm)
# ============================================================
defaults:
  - base

augmentation:
  enabled: false
