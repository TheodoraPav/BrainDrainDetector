# ============================================================
# Experiment 1 — Baseline (no augmentation)
# ============================================================
defaults:
  - base

augmentation:
  enabled: false
