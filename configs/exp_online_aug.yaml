# ============================================================
# Experiment 3 — Online Augmentation
# Noise is applied dynamically inside the DataLoader each epoch.
# Each epoch sees a different noisy version of the data.
# ============================================================
defaults:
  - base

augmentation:
  enabled: true
  mode: "online"
