# ============================================================
# Experiment 4 — VA Regression + Offline Augmentation
#
# Same as exp_va_baseline but uses pre-augmented tensors from
# data_processed/windows_aug/ (built by step 04 with
# augmentation.enabled: true).
# ============================================================
defaults:
  - base

task:
  mode: "regression_va"

training:
  selection_metric: "ccc_mean"

augmentation:
  enabled: true
